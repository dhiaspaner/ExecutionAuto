# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Environments
.venv/
venv/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Reconciliation outputs — result workbooks may contain client data
out/
*_results_*.xlsx

# Connection details are prompted for at runtime and belong in no file. These
# stay as a safety net in case anyone ever creates one locally.
config/connections.toml
config/*.local.toml

# OS noise
.DS_Store
Thumbs.db
